# logs

日次ログの保存先。`/log` コマンドが自動で `YYYY/MM/YYYY-MM-DD.md` 形式で生成する。

**このディレクトリの中身は `.gitignore` で除外されている。** プライバシー保護のため、ログ本体は手元のマシンにのみ存在する。

公開しているのは:

- `CLAUDE.md` のルールとサイクル
- `templates/` のテンプレート
- `goals/` の週次目標
- `.claude/commands/` のスラッシュコマンド定義

つまり「フレームワーク」と「目標」は公開、「日々の中身」はローカル限定。
